var AccountJScripts = function() {
    var globalURLEnlaceme = localStorage.getItem(globalURLEnlaceme);
        var UpdateProfileInformation = function(){            
            $('#form-settings-information').submit(function(event){
                var data = new FormData(this);
                $.ajax({
                    url: globalURLEnlaceme + 'account/updateAccountProfile/',
                    data: data,
                    processData: false,
                    contentType: false,
                    type: 'POST',
                    beforeSend: function(){
                        /*if((vCodActivity == '') || (vCodActivity == 0) || (vCodActivity == null)){
                            alert('Debe seleccionar la actividad económica!');
                            return false;
                        }
                        if((vServiceName == '') || (vServiceName == 0) || (vServiceName == null)){
                            alert('Debe registrar el nombre del servicio!');
                            return false;
                        }*/
                    },
                    success: function(resultUpdateProfileInformation){
                        alert(resultUpdateProfileInformation);
                        $('#personal_info_1').fadeOut(500, function(){
                            $(this).html('<div class="alert alert-success"><strong>¡Actualización correcta!</strong> Los datos del perfil se actualizarón de forma correcta, en momentos se actualizará la página.</div>').fadeIn().delay(2000);
                            setTimeout(function(){
                                    window.location.href = globalURLEnlaceme + 'account';
                                }, 3000);
                        });
                    }
                });
            event.preventDefault();              
            });
        };
        var InsertProfileLocation = function(){            
            $('#form-settings-location').submit(function(event){
                var data = new FormData(this);
                $.ajax({
                    url: globalURLEnlaceme + 'account/insertLocationProfile/',
                    data: data,
                    processData: false,
                    contentType: false,
                    type: 'POST',
                    beforeSend: function(){
                        /*if((vCodActivity == '') || (vCodActivity == 0) || (vCodActivity == null)){
                            alert('Debe seleccionar la actividad económica!');
                            return false;
                        }
                        if((vServiceName == '') || (vServiceName == 0) || (vServiceName == null)){
                            alert('Debe registrar el nombre del servicio!');
                            return false;
                        }*/
                    },
                    success: function(resultInsertProfileLocation){
                        $('#location_info_1').fadeOut(500, function(){
                            $(this).html('<div class="alert alert-info"><strong>¡Registro correcto!</strong> Los datos de ubicación se registrarón de forma correcta, en momentos se actualizará la página.</div>').fadeIn().delay(2000);
                            setTimeout(function(){
                                    window.location.href = globalURLEnlaceme + 'account';
                                }, 3000);
                        });
                    }
                });
            event.preventDefault();              
            });
        };
        var UpdateProfileLocation = function(){            
            $('#form-settings-updlocation').submit(function(event){
                var data = new FormData(this);
                $.ajax({
                    url: globalURLEnlaceme + 'account/updateLocationProfile/',
                    data: data,
                    processData: false,
                    contentType: false,
                    type: 'POST',
                    beforeSend: function(){
                        /*if((vCodActivity == '') || (vCodActivity == 0) || (vCodActivity == null)){
                            alert('Debe seleccionar la actividad económica!');
                            return false;
                        }
                        if((vServiceName == '') || (vServiceName == 0) || (vServiceName == null)){
                            alert('Debe registrar el nombre del servicio!');
                            return false;
                        }*/
                    },
                    success: function(resultUpdateProfileLocation){
                        $('#location_info_1').fadeOut(500, function(){
                            $(this).html('<div class="alert alert-success"><strong>¡Actualización correcta!</strong> Los datos de ubicación se actualizarón de forma correcta, en momentos se actualizará la página.</div>').fadeIn().delay(2000);
                            setTimeout(function(){
                                    window.location.href = globalURLEnlaceme + 'account';
                                }, 3000);
                        });
                    }
                });
            event.preventDefault();              
            });
        };
        var InsertProfilePhones = function(){            
            $('#form-settings-phones').submit(function(event){
                var data = new FormData(this);
                $.ajax({
                    url: globalURLEnlaceme + 'account/insertPhonesProfile/',
                    data: data,
                    processData: false,
                    contentType: false,
                    type: 'POST',
                    beforeSend: function(){
                        /*if((vCodActivity == '') || (vCodActivity == 0) || (vCodActivity == null)){
                            alert('Debe seleccionar la actividad económica!');
                            return false;
                        }
                        if((vServiceName == '') || (vServiceName == 0) || (vServiceName == null)){
                            alert('Debe registrar el nombre del servicio!');
                            return false;
                        }*/
                    },
                    success: function(resultInsertProfilePhones){
                        $('#location_info_2').fadeOut(500, function(){
                            $(this).html('<div class="alert alert-info"><strong>¡Registro correcto!</strong> El número seleccionado se registro de forma correcta, en momentos se actualizará la página.</div>').fadeIn().delay(2000);
                            setTimeout(function(){
                                    window.location.href = globalURLEnlaceme + 'account';
                                }, 3000);
                        });
                    }
                });
            event.preventDefault();              
            });
        };
        var GetProfilePhoneForUpdate = function(){           
            $('[id^=updatePhone-]').click(function(event){
                if(confirm('¿Realmente deseas modificar el telefono del perfil?') == true){
                    document.getElementById('vCodeModal').value = $(this).attr('idCod');
                    document.getElementById('vTypePhoneModal').value = $(this).attr('idType');
                    document.getElementById('vNumberPhoneModal').value = $(this).attr('idNumber');
                    return true;
                } else {
                    return false;
                }                 
                event.preventDefault();
            });
        };
        var GetProfilePhoneForDelete = function(){           
            $('[id^=deletePhone-]').click(function(event){
                if(confirm('¿Realmente deseas eliminar este telefono del perfil?') == true){
                    return true;
                } else {
                    return false;
                }                 
                event.preventDefault();
            });
        };
        var UpdateProfilePhone = function(){            
            $('#form-settings-updateProfilePhone').submit(function(event){
                var data = new FormData(this);
                $.ajax({
                    url: globalURLEnlaceme + 'account/updatePhoneProfile/',
                    data: data,
                    processData: false,
                    contentType: false,
                    type: 'POST',
                    beforeSend: function(){

                    },
                    success: function(resultUpdateProfilePhone){                        
                        $('#UpdatePhoneProfile').modal('hide');                        
                        $('#location_info_2').fadeOut(500, function(){
                            $(this).html('<div class="alert alert-success"><strong>¡Actualización correcta!</strong> El número seleccionado se modifico de forma correcta, en momentos se actualizará la página.</div>').fadeIn().delay(2000);
                            setTimeout(function(){
                                    window.location.href = globalURLEnlaceme + 'account';
                                }, 3000);
                        });
                    }
                });
            event.preventDefault();              
            });
        };
        var UpdateProfilePassword = function(){            
            $('#form-settings-password').submit(function(event){
                var data = new FormData(this);
                $.ajax({
                    url: globalURLEnlaceme + 'account/updatePasswordProfile/',
                    data: data,
                    processData: false,
                    contentType: false,
                    type: 'POST',
                    beforeSend: function(){

                    },
                    success: function(resultUpdateProfilePassword){                        
                        $('#account_password').fadeOut(500, function(){
                            $(this).html('<div class="alert alert-success"><strong>¡Actualización correcta!</strong> La contraseña fue modificada de forma correcta, en momentos se actualizará la página.</div>').fadeIn().delay(2000);
                            setTimeout(function(){
                                    window.location.href = globalURLEnlaceme + 'account';
                                }, 3000);
                        });
                    }
                });
            event.preventDefault();              
            });
        };    
	return {
		init: function() {
			UpdateProfileInformation(), InsertProfileLocation(), UpdateProfileLocation(), InsertProfilePhones(), GetProfilePhoneForUpdate(), GetProfilePhoneForDelete(), UpdateProfilePhone(), UpdateProfilePassword();
		}
	}
}();
jQuery(document).ready(function() {
	AccountJScripts.init()
});