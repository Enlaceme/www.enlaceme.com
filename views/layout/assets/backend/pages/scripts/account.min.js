var AccountJScripts = function() {

    var globalURLEnlaceme = localStorage.getItem(globalURLEnlaceme);    
    
        var UserScript = function() {
            $('#form-accountdata').submit(function(event){
                alert('enviar form-accountdata');
                var data = new FormData(this);
                $.ajax({
                    url: globalURLEnlaceme + 'actionprofile/actionAccountData/',
                    data: data,
                    processData: false,
                    contentType: false,
                    type: 'POST',
                    beforeSend: function(){
                    },
                    success: function(resultUserScriptMethod){
                        alert('Respuesta: ' + resultUserScriptMethod);
                        window.location = globalURLEnlaceme + 'profile/account/';
                        /*if(resultLoginMethod == 0){
                            alert('Correo no registrado en nuestro sistema, por favor registrese.');
                        } else if(resultLoginMethod == 1){
                            alert('Correo registrado y habilitado en la base de datos.');
                            
                            window.location = globalURLEnlaceme + 'admin';
                        }*/

                    }
                });
            event.preventDefault();              
            });
        };   
    
	return {
		init: function() {
			UserScript();
		}
	}
}();
jQuery(document).ready(function() {
	AccountJScripts.init()
});